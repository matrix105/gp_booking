{
  "routes": [
    {
      "method": "GET",
      "path": "/available-bookings",
      "handler": "available-booking.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/available-bookings/count",
      "handler": "available-booking.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/available-bookings/:id",
      "handler": "available-booking.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/available-bookings",
      "handler": "available-booking.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/available-bookings/:id",
      "handler": "available-booking.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/available-bookings/:id",
      "handler": "available-booking.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
